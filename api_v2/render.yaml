# Render용 배포 설정
services:
  - type: web
    name: chess-backend
    runtime: python
    buildCommand: cd api_v2 && pip install -r requirements.txt
    startCommand: cd api_v2 && uvicorn main:app --host 0.0.0.0 --port 10000
    envVars:
      - key: PYTHON_VERSION
        value: 3.10.0
